apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "brignext.observer.fullname" . }}
  labels:
    {{- include "brignext.labels" . | nindent 4 }}
    {{- include "brignext.observer.labels" . | nindent 4 }}
