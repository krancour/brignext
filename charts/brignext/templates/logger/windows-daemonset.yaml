apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: {{ include "brignext.logger.windows.fullname" . }}
  labels:
    {{- include "brignext.labels" . | nindent 4 }}
    {{- include "brignext.logger.windows.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "brignext.selectorLabels" . | nindent 6 }}
      {{- include "brignext.logger.windows.labels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "brignext.selectorLabels" . | nindent 8 }}
        {{- include "brignext.logger.windows.labels" . | nindent 8 }}
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/logger/secret.yaml") . | sha256sum }}
    spec:
      serviceAccount: {{ include "brignext.logger.fullname" . }}
      containers:
      - name: logger
        image: {{ .Values.logger.windows.image.repository }}:{{ .Values.logger.windows.image.tag }}
        imagePullPolicy: {{ .Values.logger.windows.image.pullPolicy }}
        volumeMounts:
        - name: var-log
          mountPath: /var/log
        - name: docker-containers
          mountPath: /ProgramData/docker/containers
          readOnly: true
        - name: logger-config
          mountPath: /fluentd/etc
      volumes:
      - name: var-log
        hostPath:
          path: /var/log
      - name: docker-containers
        hostPath:
          path: /ProgramData/docker/containers
      - name: logger-config
        secret:
          secretName: {{ include "brignext.logger.fullname" . }}
      {{- with .Values.logger.windows.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.logger.windows.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
